#intro
print("Welcome to the tip calculator")
#question the total bill
total_bill = input("What was the total bill? $")
#qiestion the % of the tip
percentage_tip = input("What percentage tip you like to give? 10, 12, or 15? ")
#question total people
total_people = input("How many people to slit the bill? ")

bill_plus_tip = float(total_bill) * (float(percentage_tip)/100)
result = (float(total_bill) + bill_plus_tip) / int(total_people)

end_result = "{:.2f}".format(result)

print(f"Each person should pay: ${end_result}")
